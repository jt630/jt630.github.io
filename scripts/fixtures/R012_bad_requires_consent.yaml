# Fixture R012: violates R012 — requires_consent: points to an unregistered consent key.
id: R012_bad_requires_consent
name: Fixture — Unregistered requires_consent Key
region: arm
group: fabrication
description: |
  TRIGGER when testing R012.
  SKIP never.
hardware:
  slot: active-tool
preconditions:
  - battery_pct >= 10
owned_by: jarvis
fallback:
  - trigger: something_failed
    recovery:
      - hold: build_area_restricted
        until: caregiver_ack
        requires_consent: not_a_real_consent_key    # R012: not in MORALITY.md consent keys
